<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.jeecg.modules.land.archive.document.mapper.DocAttachmentMapper">

    <select id="selectByDoc" resultType="org.jeecg.modules.land.archive.document.entity.DocAttachment">
        SELECT t.*
        FROM `t_doc_attachment` t
        WHERE t.`del_flag` = 0 AND t.`doc_type` = #{docType} AND t.`doc_id` = #{docId}
        ORDER BY t.`sort_no`, t.`upload_time`
    </select>

    <select id="selectByDocs" resultType="org.jeecg.modules.land.archive.document.entity.DocAttachment">
        SELECT t.*
        FROM `t_doc_attachment` t
        WHERE t.`del_flag` = 0
          AND t.`doc_type` = #{docType}
          AND t.`doc_id` IN
        <foreach collection="docIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY t.`doc_id`, t.`sort_no`, t.`upload_time`
    </select>

</mapper>
